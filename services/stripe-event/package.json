{
  "name": "@karat/stripe",
  "version": "1.0.0",
  "scripts": {
    "test": "serverless-bundle test",
    "clean": "rm -rf node_modules && rm -rf ./dist",
    "deploy-local": "serverless deploy --stage local",
    "deploy:transactionHandler": "sls deploy function -f transactionHandler",
    "build": "echo lambdas are not built locally"
  },
  "author": "Arjun",
  "dependencies": {
    "graphql-request": "^3.4.0",
    "stripe": "^8.129.0",
    "aws-sdk": "^2.819.0",
    "serverless": "^2.17.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.68",
    "serverless-bundle": "^4.1.0",
    "serverless-domain-manager": "^5.1.0",
    "serverless-localstack": "^0.4.28",
    "serverless-offline-aws-eventbridge": "^1.3.5"
  }
}
